<UserControl
    x:Class="OrchardWin.App.Controls.StatTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- A dashboard headline tile: icon, title, big value, small detail line. Mirrors
         Orchard's DashboardView.statTile. -->
    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <FontIcon x:Name="IconGlyph" Grid.Column="0" FontSize="20" Opacity="0.6" VerticalAlignment="Top" />
            <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock x:Name="TitleTextBlock" FontWeight="SemiBold" FontSize="13" />
                <TextBlock x:Name="ValueTextBlock" FontSize="15" FontWeight="SemiBold" FontFamily="Cascadia Mono,Consolas" />
                <TextBlock x:Name="DetailTextBlock" FontSize="11" Opacity="0.65" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
